package main

private fun sum_ary(a: array<i32>) {
    return match a {
        [] -> 0,
        [hd, ..tl] -> hd + sum_ary(tl)
    }
}

fun main() {
    final ary := [1, 2, 3, 4, 5]

    writeln(sum_ary(ary))
}
