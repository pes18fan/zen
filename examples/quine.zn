// This program prints itself as its output.
use 'string';
val b = string\asciichar(92);
val s =
    \\// This program prints itself as its output.
    \\use 'string';
    \\val b = string\asciichar(92);
    \\val s =
    \\
    \\;
    \\val l = string\split_lines(s);
    \\for var i = 0; i < 4; i = i + 1 {
    \\    puts(l[i])
    \\};
    \\for var i = 0; i < len(l); i = i + 1 {
    \\    puts('    ' .. b .. b .. l[i])
    \\};
    \\for var i = 5; i < len(l); i = i + 1 {
    \\    puts(l[i])
    \\}
;
val l = string\split_lines(s);
for var i = 0; i < 4; i = i + 1 {
    puts(l[i])
};
for var i = 0; i < len(l); i = i + 1 {
    puts('    ' .. b .. b .. l[i])
};
for var i = 5; i < len(l); i = i + 1 {
    puts(l[i])
}
