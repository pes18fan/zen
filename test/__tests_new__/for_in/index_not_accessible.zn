
// Not sure how I should go about making __idx (and __iter) inaccessible,
// I think its just a limitation of the single-pass compiler
for x in [1, 2, 3] {
    puts(__idx) // ERR: Undefined variable '__idx'
}
