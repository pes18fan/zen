// Nested Results should unwrap cleanly through multiple layers,
// with results wrapping other results.
use "result";

val unwrap = result\unwrap;

val nested = ok(ok(5));
puts(unwrap(unwrap(nested))); // expect: 5

func wrap_twice(x) {
    ok(ok(x))
};

puts(unwrap(unwrap(wrap_twice("deep")))); // expect: deep
