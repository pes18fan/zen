use "list"

var fns = []
for var i = 0; i < 3; i = i + 1 {
    list.push(fns, func() => i)
}
// Each closure should capture its own i
for f in fns {
    puts(f())
}
// expect: 0
// expect: 1
// expect: 2
